const stages=['CREATED','RISK APPROVED','SUBMITTED','PARTIAL FILL','FILLED','EXIT SIGNAL','CLOSED'];
const screens=[
  {id:'market',name:'Market Structure Agent',symbol:'NIFTY 50',signal:'Bullish BOS',side:'BUY',confidence:78,entry:24560,sl:24518,target:24642,pnl:1260},
  {id:'micro',name:'Microstructure Agent',symbol:'BANKNIFTY',signal:'Bid imbalance',side:'WATCH',confidence:69,entry:51840,sl:51760,target:52010,pnl:740},
  {id:'fno',name:'F&O Intelligence Agent',symbol:'NIFTY OPT',signal:'Short covering',side:'BUY',confidence:73,entry:186.4,sl:171.2,target:214.8,pnl:2120},
  {id:'mean',name:'Mean Reversion Agent',symbol:'RELIANCE',signal:'VWAP reclaim',side:'BUY',confidence:66,entry:2978,sl:2958,target:3016,pnl:580},
  {id:'momentum',name:'Momentum Agent',symbol:'HDFCBANK',signal:'Trend expansion',side:'BUY',confidence:71,entry:1768,sl:1752,target:1798,pnl:920},
  {id:'risk',name:'Risk & Cost Agent',symbol:'PORTFOLIO',signal:'Trade approved',side:'VETO READY',confidence:100,entry:0,sl:0,target:0,pnl:0},
  {id:'recon',name:'Execution Reconciler',symbol:'BROKER LINK',signal:'State matched',side:'SYNC',confidence:100,entry:0,sl:0,target:0,pnl:0}
];
const state={running:false,index:-1,lastMove:Date.now(),retry:0,orders:[],capital:1000000,cash:842500,pnl:4850,layout:0,delayedOnce:false,dragId:null,alerts:[]};
const $=id=>document.getElementById(id);
const layouts=[['layout-command','Command'],['layout-wall','Wall'],['layout-focus','Focus']];

function money(v){return new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(v)}
function now(){return new Date().toLocaleTimeString('en-IN',{hour12:false})}
function renderMetrics(){const rows=[['Virtual Capital',money(state.capital)],['Available Cash',money(state.cash)],['Today P&L',`${state.pnl>=0?'+':''}${money(state.pnl)}`],['Daily Drawdown','0.31%'],['Active Agents','7 / 7'],['Production Ready','20%']];$('metrics').innerHTML=rows.map(([a,b],i)=>`<article class="metric glass"><span>${a}</span><strong class="${i===2?'positive':''}">${b}</strong></article>`).join('')}
function createSeries(base,seed,count=48){let p=base||100;const out=[];for(let i=0;i<count;i++){const wave=Math.sin((i+seed)*.38)*.0024,drift=(seed%2?1:-.2)*.00045,open=p,close=open*(1+wave+drift+(Math.random()-.5)*.0022),high=Math.max(open,close)*(1+Math.random()*.0018),low=Math.min(open,close)*(1-Math.random()*.0018);out.push({open,high,low,close});p=close}return out}
function drawChart(canvas,data){const dpr=window.devicePixelRatio||1,rect=canvas.getBoundingClientRect();if(!rect.width||!rect.height)return;canvas.width=rect.width*dpr;canvas.height=rect.height*dpr;const ctx=canvas.getContext('2d');ctx.scale(dpr,dpr);const w=rect.width,h=rect.height;ctx.clearRect(0,0,w,h);const values=data.flatMap(d=>[d.high,d.low]),min=Math.min(...values),max=Math.max(...values),span=max-min||1;ctx.strokeStyle='rgba(77,226,255,.08)';for(let i=1;i<5;i++){ctx.beginPath();ctx.moveTo(0,h*i/5);ctx.lineTo(w,h*i/5);ctx.stroke()}const step=w/data.length,cw=Math.max(2,step*.56);data.forEach((d,i)=>{const x=i*step+step/2,y=v=>h-((v-min)/span)*(h-22)-11,up=d.close>=d.open;ctx.strokeStyle=up?'#37f7a4':'#ff5f78';ctx.fillStyle=ctx.strokeStyle;ctx.beginPath();ctx.moveTo(x,y(d.high));ctx.lineTo(x,y(d.low));ctx.stroke();ctx.fillRect(x-cw/2,Math.min(y(d.open),y(d.close)),cw,Math.max(1,Math.abs(y(d.open)-y(d.close))))});const last=data[data.length-1],ly=h-((last.close-min)/span)*(h-22)-11;ctx.strokeStyle='rgba(77,226,255,.7)';ctx.setLineDash([5,5]);ctx.beginPath();ctx.moveTo(0,ly);ctx.lineTo(w,ly);ctx.stroke();ctx.setLineDash([])}
function cardTemplate(s,i){return `<article class="screen-card" draggable="true" data-id="${s.id}" data-screen="${i}"><header class="screen-head"><div class="screen-title"><span class="screen-index">0${i+1}</span><div><h3>${s.name}</h3><small>${s.symbol} · ${s.signal}</small></div></div><div class="screen-tools"><button class="detail-btn" data-detail="${i}">DATA</button><button class="focus-btn" data-focus="${i}">MAX</button></div></header><div class="screen-body"><div class="chart-wrap"><canvas id="chart-${s.id}"></canvas><div class="chart-overlay"><span class="mini-tag">${s.side}</span><span class="mini-tag">CONF ${s.confidence}%</span><span class="mini-tag">LIVE</span></div></div><aside class="trade-data"><div class="data-row"><span>ENTRY</span><strong>${s.entry?s.entry.toLocaleString('en-IN'):'CONTROL'}</strong></div><div class="data-row"><span>STOP</span><strong>${s.sl?s.sl.toLocaleString('en-IN'):'HARD VETO'}</strong></div><div class="data-row"><span>TARGET</span><strong>${s.target?s.target.toLocaleString('en-IN'):'SYNC'}</strong></div><div class="data-row"><span>PAPER P&L</span><strong class="positive">${s.pnl?'+'+money(s.pnl):'—'}</strong></div><div class="data-row"><span>STATUS</span><strong class="positive">ACTIVE</strong></div></aside></div></article>`}
function renderScreens(){const saved=JSON.parse(localStorage.getItem('awt-layout')||'null');if(saved?.order){screens.sort((a,b)=>saved.order.indexOf(a.id)-saved.order.indexOf(b.id));state.layout=saved.layout||0;const [cls,label]=layouts[state.layout];$('screenGrid').className=`screen-grid ${cls}`;$('layoutLabel').textContent=`Layout: ${label}`}screens.forEach((s,i)=>s.series=s.series||createSeries(s.entry||100+i*15,i+2));$('screenGrid').innerHTML=screens.map(cardTemplate).join('');bindCards();refreshCharts()}
function bindCards(){document.querySelectorAll('.focus-btn').forEach(b=>b.onclick=()=>toggleFocus(Number(b.dataset.focus)));document.querySelectorAll('.detail-btn').forEach(b=>b.onclick=()=>openAgentDetail(Number(b.dataset.detail)));document.querySelectorAll('.screen-card').forEach(card=>{card.addEventListener('dragstart',()=>{state.dragId=card.dataset.id;card.classList.add('dragging')});card.addEventListener('dragend',()=>{card.classList.remove('dragging');document.querySelectorAll('.screen-card').forEach(c=>c.classList.remove('drag-over'))});card.addEventListener('dragover',e=>{e.preventDefault();card.classList.add('drag-over')});card.addEventListener('dragleave',()=>card.classList.remove('drag-over'));card.addEventListener('drop',e=>{e.preventDefault();const from=screens.findIndex(s=>s.id===state.dragId),to=screens.findIndex(s=>s.id===card.dataset.id);if(from>=0&&to>=0&&from!==to){const [moved]=screens.splice(from,1);screens.splice(to,0,moved);renderScreens();pushAlert('Workspace reordered','Agent screens were rearranged.','good')}})})}
function refreshCharts(){screens.forEach(s=>{if(!s.series)return;const a=s.series[s.series.length-1],open=a.close,close=open*(1+(Math.random()-.46)*.0024);s.series.push({open,close,high:Math.max(open,close)*(1+Math.random()*.0012),low:Math.min(open,close)*(1-Math.random()*.0012)});s.series.shift();const c=$(`chart-${s.id}`);if(c)drawChart(c,s.series)})}
function toggleFocus(index){const cards=[...document.querySelectorAll('.screen-card')],target=cards[index],active=target.classList.contains('focused');cards.forEach(c=>c.classList.remove('focused'));if(!active)target.classList.add('focused');setTimeout(refreshCharts,80)}
function resetFocus(){document.querySelectorAll('.screen-card').forEach(c=>c.classList.remove('focused'));setTimeout(refreshCharts,60)}
function cycleLayout(){state.layout=(state.layout+1)%layouts.length;const [cls,label]=layouts[state.layout];$('screenGrid').className=`screen-grid ${cls}`;$('layoutLabel').textContent=`Layout: ${label}`;setTimeout(refreshCharts,80)}
function saveLayout(){localStorage.setItem('awt-layout',JSON.stringify({layout:state.layout,order:screens.map(s=>s.id)}));showToast('Layout saved on this device');pushAlert('Layout saved','Current screen order and layout were stored locally.','good')}
function showToast(text){$('toast').textContent=text;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),1800)}
function renderLifecycle(){$('lifecycle').innerHTML=stages.map((s,i)=>{const cls=i<state.index?'complete':i===state.index?'active':'';return `<div class="stage ${cls}"><strong>${s}</strong><small>${i<state.index?'✓ done':i===state.index?'processing':'waiting'}</small></div>`}).join('')}
function renderAgents(){$('agents').innerHTML=screens.slice(0,6).map(a=>`<div class="agent"><h3>${a.name}</h3><p>Status: <strong>ACTIVE</strong></p><p>Signal: <strong>${a.signal}</strong></p><p>Confidence: <strong>${a.confidence}%</strong></p></div>`).join('')}
function renderRisk(){const meters=[['Daily loss',10],['Gross exposure',22],['Sector concentration',18],['Correlation risk',24],['Margin usage',16]];$('riskMeters').innerHTML=meters.map(([n,v])=>`<div class="meter"><div class="meter-label"><span>${n}</span><strong>${v}%</strong></div><div class="meter-line"><div class="meter-fill" style="width:${v}%"></div></div></div>`).join('')}
function renderOrders(){$('ordersBody').innerHTML=state.orders.map((o,i)=>`<tr data-order="${i}"><td>${o.time}</td><td>${o.symbol}</td><td>${o.agent}</td><td>${o.side}</td><td>${o.qty}</td><td>${o.stage}</td><td>${o.price}</td><td class="${o.pnl!=='—'?'positive':''}">${o.pnl}</td><td>${o.reason}</td></tr>`).join('');document.querySelectorAll('[data-order]').forEach(r=>r.onclick=()=>openOrderDetail(Number(r.dataset.order)))}
function addOrder(reason='Normal transition'){const stage=stages[Math.max(0,state.index)],price=stage==='CLOSED'?'24,585.20':'24,560.00',pnl=stage==='CLOSED'?'+₹1,260':'—';state.orders.unshift({time:now(),symbol:'NIFTY-PAPER',agent:'Market Structure',side:'BUY',qty:50,stage,price,pnl,reason,confidence:78,risk:'0.42%',cost:'₹86 estimated'});state.orders=state.orders.slice(0,12);renderOrders()}
function openDrawer(id){$(id).classList.add('open');$(id).setAttribute('aria-hidden','false')}
function closeDrawer(id){$(id).classList.remove('open');$(id).setAttribute('aria-hidden','true')}
function openAgentDetail(i){const s=screens[i];$('drawerTitle').textContent=s.name;$('tradeDetails').innerHTML=[['Symbol',s.symbol],['Signal',s.signal],['Action',s.side],['Confidence',`${s.confidence}%`],['Entry',s.entry||'Control only'],['Stop',s.sl||'Hard veto'],['Target',s.target||'Synchronized'],['Paper P&L',s.pnl?money(s.pnl):'—']].map(([a,b])=>`<div class="detail-block"><span>${a}</span><strong>${b}</strong></div>`).join('');openDrawer('tradeDrawer')}
function openOrderDetail(i){const o=state.orders[i];$('drawerTitle').textContent=`${o.symbol} · ${o.stage}`;$('tradeDetails').innerHTML=[['Agent',o.agent],['Time',o.time],['Side / Quantity',`${o.side} ${o.qty}`],['Paper Price',o.price],['Paper P&L',o.pnl],['Decision reason',o.reason],['Model confidence',`${o.confidence}%`],['Risk allocation',o.risk],['Expected friction',o.cost]].map(([a,b])=>`<div class="detail-block"><span>${a}</span><strong>${b}</strong></div>`).join('');openDrawer('tradeDrawer')}
function pushAlert(title,message,type='warn'){state.alerts.unshift({time:now(),title,message,type});state.alerts=state.alerts.slice(0,20);renderAlerts()}
function renderAlerts(){$('alertCount').textContent=state.alerts.length;$('alertList').innerHTML=state.alerts.length?state.alerts.map(a=>`<div class="alert-item ${a.type}"><strong>${a.title}</strong><small>${a.time} · ${a.message}</small></div>`).join(''):'<div class="alert-item good"><strong>All clear</strong><small>No operational alerts.</small></div>'}
function moveNext(reason='Normal transition'){state.lastMove=Date.now();state.retry=0;state.index++;$('lastTransition').textContent=now();$('watchdogState').textContent='HEALTHY';$('recoveryAction').textContent='None';addOrder(reason);renderLifecycle();if(state.index>=stages.length-1){state.running=false;state.pnl+=1260;state.cash+=1260;$('activeTradeTitle').textContent='NIFTY paper trade completed';$('startTradeBtn').disabled=false;renderMetrics();pushAlert('Paper trade closed','Lifecycle completed without unresolved state.','good')}}
function startTrade(){if(state.running)return;state.running=true;state.index=-1;state.retry=0;state.lastMove=Date.now();state.delayedOnce=false;$('activeTradeTitle').textContent='NIFTY paper trade in progress';$('startTradeBtn').disabled=true;moveNext('Trade intent created');pushAlert('New paper trade','Market Structure Agent created a trade intent.','good')}
function engineTick(){$('clock').textContent=now();$('latencyValue').textContent=`${14+Math.floor(Math.random()*14)} ms`;if(!state.running)return;const elapsed=Date.now()-state.lastMove,normalDelay=2500,timeout=6200;if(elapsed>=timeout){state.retry++;$('watchdogState').textContent='RECOVERING';$('recoveryAction').textContent=`Retry ${state.retry}: reconcile and advance`;state.lastMove=Date.now();pushAlert('Watchdog recovery',`Retry ${state.retry} started after stage timeout.`,'warn');if(state.retry<=2)setTimeout(()=>moveNext('Watchdog reconciliation recovery'),650);else{$('watchdogState').textContent='BLOCKED SAFELY';$('recoveryAction').textContent='Operator review required';$('systemStatus').textContent='SYSTEM DEGRADED';$('systemStatus').className='badge warning';state.running=false;$('startTradeBtn').disabled=false;pushAlert('Trade blocked safely','Automatic recovery limit reached; no duplicate order sent.','warn')}return}if(elapsed>=normalDelay){if(state.index===2&&!state.delayedOnce){state.delayedOnce=true;return}moveNext()}}

$('startTradeBtn').onclick=startTrade;$('layoutBtn').onclick=cycleLayout;$('saveLayoutBtn').onclick=saveLayout;$('focusResetBtn').onclick=resetFocus;$('alertBtn').onclick=()=>openDrawer('alertPanel');document.querySelectorAll('.close-drawer').forEach(b=>b.onclick=()=>closeDrawer(b.dataset.close));window.addEventListener('resize',()=>setTimeout(refreshCharts,80));
renderMetrics();renderLifecycle();renderScreens();renderAgents();renderRisk();renderAlerts();pushAlert('Command center online','Seven paper-trading screens are streaming.','good');setInterval(engineTick,500);setInterval(refreshCharts,1800);setTimeout(startTrade,900);